# Under Claw Work — Claude Code context

@AGENTS.md

Claude Code is a host for Under Claw Work, not a component being replaced by
it. Use the installed slash commands when the user explicitly requests a
skill:

- `/under-claw-work` — explain capabilities or route to the correct workflow.
- `/under-claw-work-plan` — run an approved Worklog Task workflow.
- `/under-claw-meta-prompt` — generate or improve only the Meta Prompt.
- `/under-claw-jarvis-plan-loop` — run the explicit iterative workflow.
- `/under-claw-jarvis-plan` — run one explicit staged plan workflow.

Use the repository CLI for canonical Task, Knowledge, Run, and audit changes.
Do not infer that naming a skill inside generated prose executed that skill.
